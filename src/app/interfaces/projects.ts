export interface Project {
  title: string;
  img:string,
  backend: {
    description: string;
  };
  frontend: {
    description: string;
  };
  differentials: string[];
  technologies: string[];
  link: String;
}
